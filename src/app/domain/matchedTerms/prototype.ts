import { TermIndexPrototype } from '../termIndex/prototype';

interface Prototype {
    termIndex: TermIndexPrototype;
    termTree: {};
    name: string;
}

export { Prototype as MatchedTermsPrototype };
